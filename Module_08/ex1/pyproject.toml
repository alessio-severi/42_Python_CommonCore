[project]
name = "matrix-analysis"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pandas",
    "matplotlib"
]

# poetry add --group dev mypy
# poetry add --group dev "pandas-stubs==2.3.2.250926"
# poetry run flake8 loading.py
[tool.poetry]
package-mode = false
[dependency-groups]
dev = [
    "mypy (>=2.3.1,<3.0.0)",
    "pandas-stubs (==2.3.2.250926)",
    "flake8 (>=7.3.0,<8.0.0)"
]
